<%= @child.first_name %>

<% @interests.each do |interest| %>
  <button><%= interest.name %></button>
<% end %>

<%= form_for @interest do |f| %>

  <%= f.label :name, 'Interest:' %>
  <%= f.text_field :name %>

  <%= f.submit 'Add interest' %>
  <!-- need jquery to preventdefault and also send child_id -->
<% end %>
