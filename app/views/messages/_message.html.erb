<li class="<%=  self_or_other(message) %>">
  <div class="avatar">
    <img src="<%= message_interlocutor(message).pf_image_1 %>" />
  </div>
  <div class="chatboxmessagecontent">
    <p><%= message.body %></p>
  </div>
</li>
<li class="<%=  self_or_other(message) %>">
  <div class="chatboxmessagetime">
  	<time datetime="<%= message.created_at %>" title="<%= message.created_at.strftime("%d %b  %Y at %I:%M%p") %>">
      <%= message_interlocutor(message).first_name %>'s Mom • <%= message.created_at.strftime("%d %b  %Y at %I:%M%p") %>
    </time>
  </div>
</li>